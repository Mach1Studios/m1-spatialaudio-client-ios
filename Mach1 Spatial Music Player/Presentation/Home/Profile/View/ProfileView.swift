import SwiftUI

struct ProfileView: View {
    var body: some View {
        Mach1View {
            Text("Profile view")
        }
    }
}

struct ProfileView_Previews: PreviewProvider {
    static var previews: some View {
        ProfileView()
    }
}
